apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - port: 80 # Port exposé au cluster / NodePort
      targetPort: 8000 # Port interne du container FastAPI
      nodePort: 30080 # Port exposé sur la machine Minikube
  type: NodePort
